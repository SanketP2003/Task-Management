import '../../domain/entities/task_entity.dart';

/// Data model for Task.
///
/// The data layer handles serialization/deserialization and mapping
/// between remote/local data formats and domain entities.
class TaskModel extends TaskEntity {
  const TaskModel();
}
